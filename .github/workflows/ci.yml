name: CI

on:
  push:
    branches: [master, main]
  pull_request:

jobs:
  redux-app:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: itunes-search-redux
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v4
        with:
          # actions ignore working-directory; point at the app's packageManager pin
          package_json_file: itunes-search-redux/package.json
      - uses: actions/setup-node@v7
        with:
          node-version: 22
          cache: pnpm
          cache-dependency-path: itunes-search-redux/pnpm-lock.yaml
      - run: pnpm install --frozen-lockfile
      - run: pnpm typecheck
      - run: pnpm lint
      - run: pnpm format:check
      - run: pnpm test
      - run: pnpm build
